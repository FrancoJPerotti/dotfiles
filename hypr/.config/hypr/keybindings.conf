#     __              __    _           ___                 
#    / /_____  __  __/ /_  (_)___  ____/ (_)___  ____ ______
#   / //_/ _ \/ / / / __ \/ / __ \/ __  / / __ \/ __ `/ ___/
#  / ,< /  __/ /_/ / /_/ / / / / / /_/ / / / / / /_/ (__  ) 
# /_/|_|\___/\__, /_.___/_/_/ /_/\__,_/_/_/ /_/\__, /____/  
#           /____/                            /____/        

binds {
    hide_special_on_workspace_change = true
}

########## Variables ##########
$mainMod = Super # super / meta / windows key
$term = kitty

########## Window/Session actions ##########
bindd = $mainMod+Shift, P, Color Picker , exec, hyprpicker -a 
bind = Alt, Q, killactive
bind = Alt, F4, exec, $scrPath/dontkillsteam.sh
bind = $mainMod, Delete, exit,
bind = $mainMod, F, togglefloating,
bind = $mainMod, G, togglegroup,
bind = $mainMod, M, exec, pkill -x wl-kbptr || wl-kbptr -o modes=floating,click -o mode_floating.source=detect -c ~/.config/wl-kbptr/config
bind = $mainMod + Shift, equal, exec, $scrPath/logoutlaunch.sh

########## Application shortcuts ##########
bind = $mainMod, SPACE, exec, $term

########## Rofi menus ##########
bind = $mainMod, RETURN, exec, pkill -x rofi || $scrPath/rofilaunch.sh d
bind = $mainMod, MINUS, exec, pkill -x rofi || $scrPath/rofilaunch.sh w
bind = $mainMod Shift, RETURN, exec, pkill -x rofi || $scrPath/rofilaunch.sh f
bindd = $mainMod, K, $d keybindings hint, exec, pkill -x rofi || $scrPath/keybinds_hint.sh c # launch keybinds hint
bindd = $mainMod Shift, E, $d emoji  picker , exec, pkill -x rofi || $scrPath/emoji-picker.sh # launch emoji picker
bindd = $mainMod Shift, G, $d glyph picker , exec, pkill -x rofi || $scrPath/glyph-picker.sh # launch glyph picker
bindd = $mainMod Shift, A, $d select rofi launcher , exec, pkill -x rofi || $scrPath/rofiselect.sh # launch select menu
bindd = $mainMod Shift, W, $d select a global wallpaper , exec, pkill -x rofi || $scrPath/wallpaper.sh -SG # launch wallpaper select menu
bindd = $mainMod+Shift, L, $d select hyprlock layout, exec, pkill -x rofi || $scrPath/hyprlock.sh --select # launch hyprlock layout select menu
bindd = $mainMod Shift, T, $d select a theme, exec, pkill -x rofi || $scrPath/themeselect.sh # launch theme select menu
bindd = $mainMod Shift, U, $d update system, exec, hyprctl dispatch exec 'systemupdate.sh' up# launch update script

########## Audio control ##########
bindl  = , F10, exec, $scrPath/volumecontrol.sh -o m
bindel = , F11, exec, $scrPath/volumecontrol.sh -o d
bindel = , F12, exec, $scrPath/volumecontrol.sh -o i
bindl  = , XF86AudioMute, exec, $scrPath/volumecontrol.sh -o m
bindl  = , XF86AudioMicMute, exec, $scrPath/volumecontrol.sh -i m
bindel = , XF86AudioLowerVolume, exec, $scrPath/volumecontrol.sh -o d
bindel = , XF86AudioRaiseVolume, exec, $scrPath/volumecontrol.sh -o i

########## Media control ##########
bindl  = , XF86AudioPlay, exec, playerctl play-pause
bindl  = , XF86AudioPause, exec, playerctl play-pause
bindl  = , XF86AudioNext, exec, playerctl next
bindl  = , XF86AudioPrev, exec, playerctl previous

########## Brightness control ##########
bindel = , XF86MonBrightnessUp, exec, $scrPath/brightnesscontrol.sh i
bindel = , XF86MonBrightnessDown, exec, $scrPath/brightnesscontrol.sh d

########## Screenshots ##########
bind = $mainMod, P, exec, $scrPath/screenshot.sh s
bind = , Print, exec, $scrPath/screenshot.sh p

########## Clipboard ##########
bindd = $mainMod, V, $d clipboard ,  exec, pkill -x rofi || $scrPath/cliphist.sh -c # launch clipboard,
bindd = $mainMod Shift, V, $d clipboard manager , exec, pkill -x rofi || $scrPath/cliphist.sh # launch clipboard Manager

########## Move/Change window focus ##########
bind = $mainMod, N, movefocus, l
bind = $mainMod, O, movefocus, r
bind = $mainMod, I, movefocus, u
bind = $mainMod, E, movefocus, d

########## Move/Change window focus in group ##########
bind = $mainMod, U, changegroupactive, b 
bind = $mainMod, Y, changegroupactive, f

########## Switch workspaces ##########
bind = $mainMod, h, workspace, 1
bind = $mainMod + Shift, slash, workspace, 2
bind = $mainMod, period, workspace, 3
bind = $mainMod, slash, workspace, 4
bind = $mainMod Mod5, N, workspace, r+1
bind = $mainMod, L, workspace, r-1

########## Move focused window to a relative workspace ##########
bind = $mainMod, END, movetoworkspace, r+1
bind = $mainMod, HOME, movetoworkspace, r-1

########## Resize windows ##########
binde = $mainMod ALT_L, N, resizeactive, -20 0
binde = $mainMod ALT_L, O, resizeactive, 20 0
binde = $mainMod ALT_L, I, resizeactive, 0 -20
binde = $mainMod ALT_L, E, resizeactive, 0 20

########## Move active window ##########
$moveactivewindow = grep -q "true" <<< $(hyprctl activewindow -j | jq -r .floating) && hyprctl dispatch moveactive
binded = $mainMod, left, Move activewindow to the right, exec, $moveactivewindow -30 0 || hyprctl dispatch movewindow l 
binded = $mainMod, right, Move activewindow to the right, exec, $moveactivewindow 30 0 || hyprctl dispatch movewindow r 
binded = $mainMod, up, Move activewindow to the right, exec, $moveactivewindow 0 -30 || hyprctl dispatch movewindow u 
binded = $mainMod, down, Move activewindow to the right, exec, $moveactivewindow 0 30 || hyprctl dispatch movewindow d 

########## Move/Resize focused window with mouse ##########
bindm = Alt, mouse:272, movewindow
bindm = Alt, mouse:273, resizewindow

########## Special workspaces ##########
bind = $mainMod, S, togglespecialworkspace, spotify
bind = $mainMod, D, togglespecialworkspace, discord
bind = $mainMod, W, togglespecialworkspace, whatsapp
bind = $mainMod, T, togglespecialworkspace, ticktick
bind = $mainMod, TAB, togglespecialworkspace, chatgpt

########## Split toggle ##########
bind = $mainMod, J, togglesplit

